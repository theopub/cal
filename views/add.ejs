<html>
  <head>
    <title>Red Calendar</title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Kings&family=Lugrasimo&family=Neonderthaw&family=Petit+Formal+Script&family=Plaster&family=Stalinist+One&family=Tangerine:wght@400;700&family=Turret+Road:wght@200;300;400;500;700;800&family=Warnes&display=swap"
      rel="stylesheet"
    />

    <style>
      body {
        font-family: Helvetica;
        background-color: rgb(101, 0, 0);
        font-size: 15px;
      }

      #menu {
        position: fixed;
        background-color: rgb(253, 106, 106);
        color: rgb(52, 0, 0);
        z-index: 10;
        bottom: 0;
        padding: 2vw;
        box-shadow: 0px 3px 12px 3px rgb(120, 0, 0);
        width: 210px;
        right: 0;
        font-family: Helvetica;
      }
      a {
        color: black;
      }

      input,
      textarea {
        width: 100%;
      }

      input[type="radio"] {
        width: 30;
      }

      input[type="checkbox"] {
        width: 30;
      }

      input[type="file"] {
        width: 300;
      }

      form {
        position: relative;
        background-color: rgb(253, 106, 106);
        padding: 3vw;
        width: 45vw;
        left: 24.5vw;
        top: 3vw;
        border-bottom: 3vw solid rgb(101, 0, 0);
      }

      #title-add {
        text-align: center;
        box-shadow: 0px 3px 12px 3px rgb(120, 0, 0);
        padding: 15px;
      }

      #cost-field {
        display: none; /* Hidden by default */
        margin-top: 10px;
      }

      #notes {
        box-shadow: 0px 3px 12px 3px rgb(120, 0, 0);
        padding: 15px;
      }

      #caution {
        box-shadow: 0px 3px 12px 3px rgb(120, 0, 0);
        padding: 15px;
      }

      @media only screen and (max-width: 750px) {
        #menu {
                    width: 96vw; 
                    right: none; 
                    left: 0; 
                }

        form {
          width: 88vw; 
          left: 2vw; 
        }
      }


    </style>
  </head>
  <body>
    <form action="/upload" method="post" enctype="multipart/form-data">
      <div id="title-add">Add an event to Red Calendar here:</div>
      <br />
      <div id="notes">
        All events will be reviewed by an editor before they are posted to the
        calendar (this could take a day or two!) <br /><br />
** We don't care much about the formatting, and in fact welcome you to play with the formatting we've suggested (what is a desription?) Example of something we would accept: a poetry reading put on by high schoolers. Example of something we wouldn't accept: something at Knockdown Center. 

**See about page for more info

Strong preference for cheap events. 

        Before submitting your event, please check that it’s not already on the
        calendar! If you notice an error in an event that’s already listed, email info@oral.pub.
        Please try to get it right the first time though&mdash;Red Cal editors need
        to make these edits manually and it’s kind of a pain!<br /><br />
        You'll receive an email confirmation after submitting. 
      </div>
      <br />
      <label for="name">Your name *</label>
      <input type="text" id="name" name="name" required />
      <br /><br />

      <label for="email">Your email *</label><br />
      <input type="email" id="email" name="email" required />
      <br /><br />

      <label for="your-url">A url where we can find you/your stuff (ie., personal webpage, social media account, etc.) [optional]</label><br />
      <input type="text" id="your-url" name="yourUrl"/>
      <br /><br />


      <label for="image"
        >Event image/poster [accepts .jpg, .png, .gif. Height to width ratio
        must be 4:3 or less. Please make sure your file is under 1MB.] *</label
      >
      <input
        type="file"
        id="image"
        name="image"
        accept="image/*"
        required
        onchange="previewImage(event)" required
      />
      <img
        id="preview"
        src=""
        alt="Image Preview"
        style="display: none; width: 100%; height: auto"
      />
      <br /><br />


      <label for="title">Event title *</label><br />
      <input type="text" id="title" name="title" required />
      <br /><br />



      <label for="cost">Cost *</label><br />
      <input type="text" id="cost" name="cost" required />
      <br /><br />

      <!-- <input
        type="radio"
        id="free"
        name="event_cost"
        value="free"
        required
        onclick="toggleCost(false)"
      />

      <label for="free">It's free or notaflof</label><br />

      <input
        type="radio"
        id="not-free"
        name="event_cost"
        value="not-free"
        required
        onclick="toggleCost(true)"
      />

      <label for="not-free">You need to pay to enter</label><br />

      <div id="cost-field">
        <label for="cost">How much?</label>
        <input
          type="number"
          id="cost"
          name="cost"
          min="0"
          step="0.01"
          placeholder="0.00"
        /><br />
      </div>
      <br /> -->

      <label for="when">Date & Time *</label><br />
      <input type="datetime-local" id="when" name="when" required />
      <br /><br />

      <label for="where"
        >Location * [Whatever you type here
        will be included as-is! Please include the neighborhood/zone, ie. "Bed-Stuy" or "Chinatown."]</label
      ><br />
      <textarea rows="3" id="where" name="where" required></textarea>
      <br /><br />

      <label
        >Which of the following describes your event (choose up to 3): *</label
      ><br />

      <% tags.forEach((t)=>{ %>
        <input type="checkbox" id="<%=t.id%>" name="categories" value="<%=t.tag_name%>">
        <label for="<%=t.id%>"><%=t.tag_name%></label><br />
      <% }) %>
     

      <p id="warning" style="border: 9px solid red; display: none">
        No more than 3 <3
      </p>
      <br />


      <label for="description"
        >Description [optional] &mdash; accessibility, participants, etc.]:</label
      ><br />
      <textarea rows="3" id="description" name="description"></textarea>
      <br /><br />


      <label for="urlurl">Url [optional] </label>
      <input type="text" id="urlurl" name="urlurl" />
      <br /><br />

      <label for="link">Link text for above url [optional] &mdash; eg. "Buy tickets!" or "RSVP here" or "Link to venue <3"</label>
      <input type="text" id="link" name="link" />
      <br /><br />

      <br /><br />
      <div id="caution">
        Before submitting your event,
        <u><b>check everything twice or thrice!</b></u> Once you click submit,
        any edits will have to be done manually by a Red Cal editor.
      </div>
      <br />
      <button type="submit">Submit and return to Calendar</button>
    </form><br><br><br><br><br><br>

    <div id="menu">
      Welcome to Red Calendar.<br /><br />

      <a href="/">Back to the Calendar.</a><br /><br />

      <a>About.</a>
    </div>


    <script>
      function previewImage(event) {
        const file = event.target.files[0];
    
        if (!file) return;
    
        // Check file size (1MB = 1,048,576 bytes)
        if (file.size > 1048576) {
          alert("This image is bigger than 1MB! Please upload an image under 1MB (online compression tools rock.)");
          event.target.value = ""; // Clear the file input
          document.getElementById("preview").style.display = "none"; // Hide preview
          return;
        }
    
        // ✅ Show preview if file size is okay
        const reader = new FileReader();
        reader.onload = function () {
          const preview = document.getElementById("preview");
          preview.src = reader.result;
          preview.style.display = "block";
        };
        reader.readAsDataURL(file);
      }
    </script>
    

    <script>
      // Get current date and time
      const now = new Date();
      
      // Set the time to 7 PM (19:00)
      now.setHours(16, 0, 0, 0); // 19:00:00.000
    
      // Format date to `YYYY-MM-DDTHH:MM` (datetime-local expected format)
      const formattedDateTime = now.toISOString().slice(0, 16); 
    
      // Set the default value in the input field
      document.getElementById("when").value = formattedDateTime;
    </script>

    <script>
      function toggleCost(show) {
        document.getElementById("cost-field").style.display = show
          ? "block"
          : "none";
      }
    </script>

    <script>
      function previewImage(event) {
        const preview = document.getElementById("preview");
        const file = event.target.files[0];

        if (file) {
          const reader = new FileReader();
          reader.onload = function () {
            preview.src = reader.result;
            preview.style.display = "block";
          };
          reader.readAsDataURL(file);
        }
      }
    </script>

    <script>
      document.addEventListener("DOMContentLoaded", function () {
        const checkboxes = document.querySelectorAll(
          'input[name="categories"]'
        );
        const warning = document.getElementById("warning");

        checkboxes.forEach((checkbox) => {
          checkbox.addEventListener("change", function () {
            const checkedBoxes = document.querySelectorAll(
              'input[name="categories"]:checked'
            );

            if (checkedBoxes.length > 3) {
              this.checked = false; // Uncheck the last clicked checkbox
              warning.style.display = "block"; // Show warning message
            } else {
              warning.style.display = "none"; // Hide warning message
            }
          });
        });
      });
    </script>
  </body>
</html>
